import { RuntimeStage } from "./RuntimeStage"

export function ConcurrentVsParallel() {
  return <RuntimeStage scene="parallel" title="core occupancy" caption="Concurrency takes turns; parallelism occupies separate cores." cue="Compare one moving marker with two simultaneous markers." />
}
